/* =========================================================
   Ajustes da assistente virtual.
   Nome, papel e ritmo de digitacao ficam aqui.
   Os textos das respostas estao em chatbot/flows.js.
   ========================================================= */

const CHAT = {
  nome: 'Lena',
  papel: 'assistente virtual',

  /* tempo do "digitando..." antes de cada resposta, em ms */
  digitandoMin: 850,
  digitandoExtra: 450
};
